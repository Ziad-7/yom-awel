# Hints

- Copy the exact email after `To:` and include the order ID after `Subject:`.
- Mention the order ID, promised date, updated date, and refund amount with EGP. Use the ISO dates shown in the case CSV.
- Include an apology, clearly say that the customer will receive a refund, and say you will respond within **2 business days**.
- Greet the customer by name and end with `Best regards, Yom Awel` or an equivalent Arabic closing. Keep the body between 30 and 350 words.
- Save plain UTF-8 text as `.txt`, not a document or spreadsheet. The line containing `To:` and the line containing `Subject:` precede a blank line and the body.
