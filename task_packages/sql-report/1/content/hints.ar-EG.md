# تلميحات

- رشّح `status` للطلبات المدفوعة **قبل** التجميع.
- تستخدم `COUNT(*)` لحساب عدد الطلبات في كل مجموعة.
- تستخدم `SUM(quantity * unit_price)` لحساب الإيراد، ثم `ROUND(..., 2)` للتقريب إلى منزلتين.
- سمّ الأعمدة الثلاثة بالضبط `region` و`paid_orders` و`total_revenue`.
